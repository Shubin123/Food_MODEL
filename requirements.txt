# Food MODEL — Python test/evaluation dependencies
# Install: pip install -r requirements.txt

# Core: ONNX inference (CPU — no GPU required for benchmarking)
onnxruntime>=1.19.0,<1.21.0
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0,<12.0.0

# Hugging Face datasets (public, no auth needed for food101 etc.)
datasets>=3.0.0,<4.0.0
huggingface_hub>=0.25.0,<1.0.0

# HTTP/image fetching
requests>=2.31.0,<3.0.0
